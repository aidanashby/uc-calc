{
  "name": "uc-calc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "jest",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jest": "^29.7.0"
  }
}
